{
  "name": "dawn-starter",
  "type": "module",
  "scripts": {
    "dev": "shopify theme dev --store $npm_package_config_store & npm run dev:tailwind",
    "dev:tailwind": "npx tailwindcss -i ./assets/base.src.css -o ./assets/base.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "config": {
    "store": "wiebe-checkout"
  },
  "homepage": "https://$npm_package_config_store.myshopify.com",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.27",
    "tailwindcss": "^3.3.3"
  }
}
